# scavenge

Grad Linux™'s package manager, idk what else to say man.

## build

to not make me struggle, i made the build in pure shell, with zero "bashisms" so that the build script would be compatible before me obtaining bash from scavenge itself.

just run `./build` and get the `./scavenge` executable! the script works with `gcc` and, of course, on Grad Linux™'s `tcc` and `musl` combo.